# Python Codes
- The python code `StereoConcatenator.py` in `scripts` is used to concatenate left and right images, compress and send to unity. Remember to change the Subscriber
- The python code `UserStudy.py` is used to perform the user study and display a figure (green square) on the right and left images.
